# Web

Historical React/Vite frontend rescued from branch `frontend` at `4d481ad922bfed8ce4dbc6418a5111cfd6daefe0`.

Important: the current authentication implementation is a prototype using `fakeAuthApi`. It is not a security boundary and must be replaced by the shared backend identity/session contract before production use.
